create or replace function xavier(xml T_CLOB) returns T_CLOB PROC_LANG as
	declare
		r T_CLOB;
		b0 T_CLOB;
	begin
		return true;
	end;
create or replace function xavier(xml T_CLOB) returns T_CLOB PROC_LANG as
$$
	declare
		r T_CLOB;
		b0 T_CLOB;
	begin
		return true;
	end;
$$;
