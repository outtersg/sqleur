{
	"name": "gui/sqleur",
	"description": "Préprocesseur & découpeur de SQL",
	"version": "1.13.240515",
	"require":
	{
		"php": ">= 5.4.0"
	},
	"suggest":
	{
		"gui/musqle": "pour le support du #create table from <autre base> (>= 2.2), du #create table into <autre base> (>= 2.3)",
		"gui/util": "pour le #create table from <autre base> (>= 1.1.231111), le sql2csv.php -e <fournisseur ext> (>= 1.2), l'#exec (>= 1.3)"
	}
}
